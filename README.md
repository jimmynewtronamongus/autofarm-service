# Grow a Garden 2 Autofarm Hub

`main.lua` is the only script used by this repository.

## Permanent Loadstring

This URL always loads the current `main.lua` from the `master` branch:

```lua
loadstring(game:HttpGet("https://raw.githubusercontent.com/jimmynewtronamongus/autofarm-service/master/main.lua"))()
```

Repository file:

`https://github.com/jimmynewtronamongus/autofarm-service/blob/master/main.lua`
